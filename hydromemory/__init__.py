"""HydroMemory Protocol — reference implementation.

A stateful memory architecture with a hydraulic model over a computational
memory lifecycle (see the PRD and ``docs/``). Submodules are imported explicitly
by callers; this package root stays import-light on purpose.
"""

__version__ = "0.1.0"
