"""HydroMemory efficacy evals (out of CI; see evals/README.md).

These measure whether the gap-closure mechanisms *help* (vs. baselines), as
opposed to the deterministic correctness suite in ``tests/`` which proves they
*run*. Run on demand: ``python -m evals.run --eval multihop``.
"""
